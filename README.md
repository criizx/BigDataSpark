# BigDataSpark

```bash
bash download_jars.sh 
make all  
```

PostgreSQL: `localhost:5432` db=`bigdata` user=`spark` password=`spark123`  
ClickHouse: `localhost:8123` db=`reports` user=`spark` password=`spark123`

Запросы для проверки: `sql/verification_queries.sql`

```bash
make down
```
